@model IEnumerable<Deerfly_Patches.Models.Retailer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LatLng.LatLngId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LatLng.LatLngId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RetailerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RetailerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RetailerId })
        </td>
    </tr>
}

</table>
